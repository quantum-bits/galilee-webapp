import { ResourceModalAnchorDirective } from './resource-modal-anchor.directive';

describe('ResourceModalAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ResourceModalAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
