import { ResourceItemModalAnchorDirective } from './resource-item-modal-anchor.directive';

describe('ResourceItemModalAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ResourceItemModalAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
