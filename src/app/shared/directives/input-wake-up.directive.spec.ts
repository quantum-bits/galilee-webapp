/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import { InputWakeUp } from './input-wake-up.directive';

describe('InputWakeUp Directive', () => {
  it('should create an instance', () => {
    let directive = new InputWakeUp();
    expect(directive).toBeTruthy();
  });
});
